%section
  %header.section.head
    %h1.left
      Manage Posts

    .actions.right
      %a.button{ href: new_post_path } New Post



  %table{ cellpadding: 0, cellspacing: 0 }
    %thead
      %tr
        %th Image
        %th Text
        %th Tweet
        %th Published
        %th Time
        %th Action
    %tbody
      - @resources.each do |r|
        %tr
          %td
            - if r.image.present?
              %a{ href: r.image, target: '_blank' }= r.image_file_name
            - else
              No image uploaded
          %td= r.text
          %td= r.tweet
          %td= r.publish?
          %td= r.publish_at.to_time
          %td
            %a.yellow.button.mini{ href: edit_post_path(r) } edit
            %a.red.button.mini{ href: post_path(r), data: { confirm: "Are you sure you want to delete?", method: :delete } } delete